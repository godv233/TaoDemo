<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RadioGroup
        android:id="@+id/test_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/test_home"
            android:checked="true"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/test_nav_home"
            android:gravity="center"
            android:text="@string/text_home"
            android:textColor="@drawable/navigation_bar_color" />


        <RadioButton
            android:id="@+id/test_select"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/test_nav_select"
            android:gravity="center"
            android:text="@string/text_recommend"
            android:textColor="@drawable/navigation_bar_color" />

        <RadioButton
            android:id="@+id/test_packet"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/test_nav_packet"
            android:gravity="center"
            android:text="@string/text_red_packet"
            android:textColor="@drawable/navigation_bar_color" />

        <RadioButton
            android:id="@+id/test_search"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/test_nav_search"
            android:gravity="center"
            android:text="@string/text_search"
            android:textColor="@drawable/navigation_bar_color" />


    </RadioGroup>

</RelativeLayout>